
import 'package:flutter/cupertino.dart';

class Apps_Demo extends StatelessWidget {
  const Apps_Demo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Apps",style: TextStyle(fontWeight: FontWeight.bold),),
      ),
    );
  }
}
